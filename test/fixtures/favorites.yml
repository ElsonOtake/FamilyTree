# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
#
# Intentionally empty: the favorites tests (and the favorites integration test,
# which asserts "no favorites yet" for user one) each create the favorites they
# need. Seeding user↔person favorites here collided with those tests
# ("User has already favorited this person").
